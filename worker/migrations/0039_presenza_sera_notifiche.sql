-- Dedup della push "ricordati di mettere la presenza" mandata alle 17:00 dei giorni di
-- allenamento a chi non ha ancora risposto (stesso pattern di allenamento_notifiche).
CREATE TABLE presenza_sera_notifiche (data TEXT PRIMARY KEY); -- "YYYY-MM-DD"
